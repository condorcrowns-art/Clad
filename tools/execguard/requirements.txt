# ExecGuard dependencies — all free and open source.
#
# Required for real monitoring (the detection core itself is pure stdlib):
psutil>=5.9            # BSD-3 — process, module and connection enumeration

# Optional, but recommended on Windows:
watchdog>=3.0          # Apache-2.0 — real-time file system events instead of polling
WMI>=1.5.1 ; sys_platform == "win32"      # MIT — event-driven process-start notifications
pywin32>=306 ; sys_platform == "win32"    # PSF — required by WMI

# Optional, build only:
# pyinstaller>=6.0      # GPL-2.0-with-exception — builds the single portable .exe
#
# The GUI uses tkinter, which ships with the official python.org Windows
# installer. Nothing here costs money and nothing requires an account.
