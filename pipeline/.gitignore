# Working folders for the conversion pipeline. Same rule as the store: nothing
# here is a deliverable, and masters must never reach a public repo.
refs/
prepped/
raw/
generated/
finished/
*.glb
*.gltf
*.fbx
*.obj
*.ply
*.blend
*.blend1

# Character pipeline working files — never commit these.
# Masters, raw generations, and un-watermarked renders all live here.
work/
