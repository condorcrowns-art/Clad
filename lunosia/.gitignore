# Downloaded by setup-windows.ps1 - hundreds of megabytes of binaries and
# voice models that have no business in a git repository.
piper/
voices/
node_modules/

# Raw sources for tools/build-dict.js. Only the built dictionary is committed;
# these are ~7 MB of upstream data that anyone can re-download.
tools/cache/
